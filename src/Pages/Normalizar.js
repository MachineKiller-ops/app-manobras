import React from 'react'

export const Normalizar = ({csvData, fileName}) => {

    const normalizar = (csvData, fileName) => {

    }

    return (

        <button  onClick={() => normalizar(csvData,fileName)}>Baixar sequência de manobras</button>

    )

}

